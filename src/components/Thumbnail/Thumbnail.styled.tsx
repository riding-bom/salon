import styled from "styled-components";
import Thumbnail from "./Thumbnail";

const StyledHeaderThumbnail = styled(Thumbnail)`
  width: 150px;
  height: 160px;
  border-radius: 50%;
`;

export default StyledHeaderThumbnail;
