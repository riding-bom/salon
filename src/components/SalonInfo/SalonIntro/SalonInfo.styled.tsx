import styled from 'styled-components';
import SalonInfo from './SalonIntro';

const StyledSalonInfo = styled(SalonInfo)`
  font-weight: 200px;
  font-size: 1.4rem;
  width: 330px;
  line-height: 1.6rem;
`;

export default StyledSalonInfo;
